# These are frontend todos
# TODO: a function using 'name' as parameter to store a new route to backend /api/user_routes*/}
# TODO: a function with no parameter to get all routes, including id and name, from the same endpoint */}
# TODO: a function with id and name as parameter to update the route with the new name, from the same endpoint */}
# Currently, no need to really store the route, just name and id */}
# Be careful of ruff and mypy rules */}
# TODO (Daniel): Backend should be a blueprint with route user_routes (register in App.py), implement frontend requests
# the db.Model class is in server.models.map_route
# It is preferable that each user has their own routes, so you may need to alter db.Model for user as well

