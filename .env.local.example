# Committer
COMMITTER="<Your Github ID, once configured do NOT modify>"
# Ports, API is the backend and DEV is the frontend, change them if the original ports are occupied
VITE_API_PORT="5050"
VITE_DEV_PORT="5173"

VITE_PROJECT_MODE="BUILD"       # BUILD or DEV
VITE_DEPLOY_TARGET="EMULATOR"   # EMULATOR or MACHINE

# Automatic Fields to be filled in by scripts
VITE_API_HOST="fill_by_script"